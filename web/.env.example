# Astro dev environment — copy to web/.env
# In production this is set as an environment variable in Cloudflare Pages.

# Local API (matches the Docker container port)
PUBLIC_API_URL=http://localhost:8080
